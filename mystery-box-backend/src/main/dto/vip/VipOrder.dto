export io.github.qifan777.server.vip.order.entity.VipOrder

input VipOrderInput {
    #allScalars(VipOrder)
    id
    id(vipPackage)
}

specification VipOrderSpec {
    #allScalars
    id(user)
    id(vipPackage)
    id(creator)
}