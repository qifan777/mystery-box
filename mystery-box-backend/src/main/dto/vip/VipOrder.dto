export io.github.qifan777.server.vip.order.entity.VipOrder

input VipOrderInput {
    #allScalars(VipOrder)
    id
    id(vipLevel)
}

specification VipOrderSpec {
    #allScalars
    id(user)
    id(vipLevel)
    id(creator)
}