export io.github.qifan777.server.product.root.entity.Product

input ProductInput {
    #allScalars(Product)
    id
    id(category)
}

specification ProductSpec {
    #allScalars
    like/i(name)
    like/i(brand)
    id(category)
    like/i(description)
    id(creator)
    boxId: String?
}